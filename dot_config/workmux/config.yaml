# Config globale workmux — surchargeable par .workmux.yaml dans chaque projet
main_branch: main
agent: claude
merge_strategy: rebase

post_create:
  - workmux setup
  - workmux sidebar

panes:
  - command: <agent>
    focus: true
  - command: nvim .
    split: horizontal
